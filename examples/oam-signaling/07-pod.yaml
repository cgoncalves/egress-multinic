# EgressIP pod -- matched by the EgressIP CR via podSelector (app: demo).
# Traffic SNAT'd to the admin-configured /32 IP on the outgoing interface.
---
apiVersion: v1
kind: Pod
metadata:
  name: egressip-pod
  namespace: demo-egressip
  labels:
    app: demo
spec:
  containers:
    - name: demo
      image: registry.k8s.io/e2e-test-images/agnhost:2.45
      command: ["sleep", "infinity"]

# Non-EgressIP pod -- NOT matched by any EgressIP CR.
# Traffic masqueraded to the host IP of the outgoing interface (when
# egress-gateway is active on worker nodes).
---
apiVersion: v1
kind: Pod
metadata:
  name: non-egressip-pod
  namespace: demo-egressip
spec:
  containers:
    - name: test
      image: registry.k8s.io/e2e-test-images/agnhost:2.45
      command: ["sleep", "infinity"]
